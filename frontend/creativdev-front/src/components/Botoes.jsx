import React, { useState, useEffect, useContext } from "react";

export const BtnSalvar = (obj) => {
   
   return(
   <button className="btn btn-primary me-2 px-3">Salvar</button>
   )
}

export const BtnFechar = (obj) => {
    return(
        <button className="btn btn-secondary px-3">Fechar</button>
        )
    
}
